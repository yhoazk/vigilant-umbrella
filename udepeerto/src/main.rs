/// Read and interpret the contents of
/// 
/// /proc/net/udp
/// /proc/net/tcp
/// /proc/net/snmp
/// run cargo check to check that the code compiles w/o generating a binary

fn main() {
    println!("Hello, world!");
}
